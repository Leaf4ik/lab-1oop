public interface TextProcessor {
    /**
     * Операция 1 — базовый метод:
     * в базовом классе — поиск пар анаграмм,
     * в конечном наследнике — удаление таких слов.
     */
    void findAnagramPairs();
}
